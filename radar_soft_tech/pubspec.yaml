name: radar_soft_tech
description: RadarSoftTech Job Assignment

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

  # For HTTP requests
  http: ^0.13.4

  # For State Management
  provider: ^6.0.1

  # Cached Network Image
  cached_network_image: ^3.1.0

flutter:

  uses-material-design: true
